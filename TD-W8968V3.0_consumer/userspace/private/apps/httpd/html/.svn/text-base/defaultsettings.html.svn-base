<html>
   <head>
      <meta HTTP-EQUIV='Pragma' CONTENT='no-cache'>
      <link rel="stylesheet" href='stylemain.css' type='text/css'>
         <link rel="stylesheet" href='colors.css' type='text/css'>
            <script language="javascript">
<!-- hide

function restoreClick() {
   var loc = 'restoreinfo.cgi?';

   loc += 'sessionKey=<%ejGetOther(sessionKey)%>';
   var code = 'location="' + loc + '"';

   if (confirm("Are you sure you want to restore factory default settings?"))
      eval(code);
}

// done hiding -->
</script>
   </head>
   <body>
      <center>
         <form>
            <table>
			<tr><td class=h1>Tools -- Restore Default Settings</td></tr>
			<tr><td class=blue></td></tr>
			<tr><td>
            Restore DSL Modem Router settings to the factory defaults.</td></tr>
			<tr><td class=blue></td></tr>
			<tr><td>
            <center><input type='button' value='Restore Default Settings' onClick='restoreClick()'></td></tr>
			</table>
         </form>
      </center>
   </body>
</html>
